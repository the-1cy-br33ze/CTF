version: "3.3"

services:
  terminal:
    build: .
    ports:
      - "5454:5454"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
    command: python app.py && tail -f /dev/null
