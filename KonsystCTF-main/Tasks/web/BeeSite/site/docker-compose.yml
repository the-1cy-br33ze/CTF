version: '3.3'
services:
  honey-site:
    image: nginx:alpine
    ports:
      - "8484:80"
    volumes:
      - ./site:/usr/share/nginx/html
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
